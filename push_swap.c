#include "push_swap.h"

int main(int argc, char **argv)
{
    t_num   num;

    num.argc = argc;
    ft_separate(&num, argv);
    ft_part(&num);
    // int i;
    // i = -1;
    // while (++i < num.counta)
    //     printf("%d ", num.num_lista[i]);
    //ft_close(&num);
}